import ValidatingForm from '../lib/components/ValidatingForm';
import React from 'react';
import { mount } from 'enzyme';

describe('ValidatingForm', () => {
    it('renders all children elements', () => {

    });
    it('wraps onSubmit handler with own handler', () => {

    })

    it('renders all ValidatingInput children with additional props', () => {
        
    })

    it('sets submit button to disabled when validation returns errors within children', () => {
        
    })
});